<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== 全局字体 ===== -->
    <FontFamily x:Key="GlobalFont">SimSun</FontFamily>
    <Style TargetType="TextElement">
        <Setter Property="FontFamily" Value="{StaticResource GlobalFont}" />
    </Style>
    <Style TargetType="Window">
        <Setter Property="FontFamily" Value="{StaticResource GlobalFont}" />
    </Style>
    <Style TargetType="UserControl">
        <Setter Property="FontFamily" Value="{StaticResource GlobalFont}" />
    </Style>

    <!-- ===== 颜色定义 ===== -->
    <SolidColorBrush x:Key="WindowBackground" Color="#F0F2F5" />
    <SolidColorBrush x:Key="PrimaryBlue" Color="#1E88E5" />
    <SolidColorBrush x:Key="PrimaryBlueHover" Color="#1976D2" />
    <SolidColorBrush x:Key="PrimaryBluePressed" Color="#1565C0" />
    <SolidColorBrush x:Key="DangerRed" Color="#F44336" />
    <SolidColorBrush x:Key="DangerRedHover" Color="#E53935" />
    <SolidColorBrush x:Key="DangerRedPressed" Color="#D32F2F" />
    <SolidColorBrush x:Key="SuccessGreen" Color="#4CAF50" />
    <SolidColorBrush x:Key="SuccessGreenHover" Color="#43A047" />
    <SolidColorBrush x:Key="SuccessGreenPressed" Color="#388E3C" />
    <SolidColorBrush x:Key="Purple" Color="#9C27B0" />
    <SolidColorBrush x:Key="DisabledGray" Color="#BDBDBD" />
    <SolidColorBrush x:Key="TextDark" Color="#333333" />
    <SolidColorBrush x:Key="TextGray" Color="#888888" />
    <SolidColorBrush x:Key="BorderLight" Color="#DDDDDD" />
    <SolidColorBrush x:Key="BorderGray" Color="#CFD8DC" />
    <SolidColorBrush x:Key="ToolbarBg" Color="#FFFFFF" />
    <SolidColorBrush x:Key="PanelBg" Color="#1E1E1E" />
    <SolidColorBrush x:Key="PanelHeaderBg" Color="#2A2A2A" />

    <!-- ===== 基础按钮样式模板（所有按钮共享 ControlTemplate） ===== -->
    <Style x:Key="BaseButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{StaticResource DisabledGray}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== 主按钮 (蓝色) ===== -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBlue}" />
        <Setter Property="BorderBrush" Value="#1565C0" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinHeight" Value="36" />
    </Style>

    <!-- ===== 危险按钮 (红色) ===== -->
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource DangerRed}" />
        <Setter Property="BorderBrush" Value="#D32F2F" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinHeight" Value="36" />
    </Style>

    <!-- ===== 成功按钮 (绿色) ===== -->
    <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource SuccessGreen}" />
        <Setter Property="BorderBrush" Value="#388E3C" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinHeight" Value="36" />
    </Style>

    <!-- ===== 工具栏按钮 (紧凑型，带间距) ===== -->
    <Style x:Key="ToolbarButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextDark}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderLight}" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Margin" Value="6,0,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E3F2FD" />
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#BBDEFB" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource DisabledGray}" />
                            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== 工具栏主按钮 (蓝色紧凑型) ===== -->
    <Style x:Key="ToolbarPrimaryButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
        <Setter Property="Background" Value="{StaticResource PrimaryBlue}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="#1565C0" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Padding" Value="14,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{StaticResource DisabledGray}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== 工具栏危险按钮 (红色紧凑型) ===== -->
    <Style x:Key="ToolbarDangerButton" TargetType="Button" BasedOn="{StaticResource ToolbarPrimaryButton}">
        <Setter Property="Background" Value="{StaticResource DangerRed}" />
        <Setter Property="BorderBrush" Value="#D32F2F" />
    </Style>

    <!-- ===== 摄像头面板按钮 (小尺寸) ===== -->
    <Style x:Key="PanelButton" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Padding" Value="10,3" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0,0,5,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== 缩放控制按钮 (面板内小圆角) ===== -->
    <Style x:Key="ZoomButton" TargetType="Button">
        <Setter Property="Width" Value="26" />
        <Setter Property="Height" Value="26" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="#FFF" />
        <Setter Property="Background" Value="#555" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="3"
                            Padding="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#777" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#999" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== 工具栏分隔符 ===== -->
    <Style x:Key="ToolbarSeparator" TargetType="Separator">
        <Setter Property="Width" Value="1" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Margin" Value="10,0" />
        <Setter Property="Background" Value="{StaticResource BorderLight}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- ===== 状态标签 ===== -->
    <Style x:Key="StatusLabel" TargetType="Label">
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryBluePressed}" />
    </Style>

    <!-- ===== 分组框 ===== -->
    <Style x:Key="GroupBoxStyle" TargetType="GroupBox">
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderLight}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="White" />
        <Setter Property="Padding" Value="8" />
    </Style>

    <!-- ===== 全局控件默认样式 ===== -->
    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="4" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="4" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="2" />
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Margin" Value="4,0" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Padding" Value="2,0" />
        <Setter Property="Margin" Value="0,2,0,0" />
    </Style>
</ResourceDictionary>